# Vercel 部署指南

## 第一步：登录 Vercel

1. 访问 https://vercel.com/login
2. 点击 **Continue with GitHub**
3. 授权 Vercel 访问你的 GitHub 账号

---

## 第二步：导入项目

1. 登录后，点击 **Add New...** → **Project**
   
2. 在 "Import Git Repository" 页面：
   - 找到 `jiangsu-superleague` 仓库
   - 点击 **Import**

---

## 第三步：配置项目

在配置页面，确认以下设置：

| 设置项 | 值 |
|--------|-----|
| **Framework Preset** | `Vite` |
| **Root Directory** | `./` |
| **Build Command** | `npm run build` |
| **Output Directory** | `dist` |

如果 Framework Preset 没有自动识别为 Vite，手动选择：
1. 点击 Framework Preset 下拉框
2. 选择 **Vite**

---

## 第四步：部署

1. 点击 **Deploy** 按钮
2. 等待 1-2 分钟构建完成
3. 看到 🎉 **Congratulations!** 表示成功

---

## 第五步：访问网站

部署成功后会获得链接：
```
https://jiangsu-superleague-xxx.vercel.app
```

点击链接即可访问你的项目！

---

## 页面导航

部署后访问各个页面：
- **P0 宏观溯源**: `https://你的链接.netlify.app/`
- **P1 全局态势**: `https://你的链接.netlify.app/p1`
- **P2 场内微观**: `https://你的链接.netlify.app/p2`
- **P3 评估闭环**: `https://你的链接.netlify.app/p3`

---

## 后续更新代码

修改代码后，推送到 GitHub 会自动触发 Vercel 重新部署：

```bash
git add .
git commit -m "更新描述"
git push
```

约 30 秒后自动生效，无需手动操作！

---

## 绑定自定义域名（可选）

1. 在 Vercel 项目页面，点击 **Settings** → **Domains**
2. 输入你的域名，如 `superleague.yourdomain.com`
3. 按提示配置 DNS 解析

---

## 常见问题

### Q: 构建失败显示 "Command failed"
A: 检查 Framework Preset 是否选择了 Vite

### Q: 页面刷新 404
A: 已配置 `vercel.json` 支持前端路由，如仍有问题请检查文件是否存在

### Q: 图片不显示
A: 确保 `public` 文件夹中的图片已上传到 GitHub

---

**部署完成后来告诉我你的链接！** 🚀
